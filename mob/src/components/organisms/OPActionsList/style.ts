import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    height: '100%',
    paddingTop: 15,
    zIndex: -1,
  },
  content: {
    flexGrow: 1,
    paddingBottom: 200,
  },
});
