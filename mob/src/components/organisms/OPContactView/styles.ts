import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  contentContainer: {
    paddingTop: 3,
    paddingBottom: 200,
    zIndex: -1,
  },
  listContainer: {
    padding: 16
  },
  separator: {
    height: 15,
  },
});
