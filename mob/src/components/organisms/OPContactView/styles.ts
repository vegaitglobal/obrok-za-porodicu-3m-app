import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  contentContainer: {
    paddingBottom: 200,
  },
  listContainer: {
    padding: 16,
  },
  separator: {
    height: 15,
  },
});
