import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  topRow: {
    flexDirection: 'row',
    marginBottom: 8,
  },
  bottomRow: {
    flexDirection: 'row',
    marginBottom: 24,
  },
  divider: {
    width: 8,
  },
});
